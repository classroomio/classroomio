name: CI

# Build + lint gate. Runs on every PR so a broken @cio/api / @cio/dashboard (or any
# workspace) build cannot merge to main undetected. Also runs on push to main.
on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "20"

jobs:
  build-and-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Setup turbo cache
        uses: actions/cache@v4
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-turbo-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Scope: build only the @cio/api and @cio/dashboard closures — the two Docker images
      # this gate exists to protect. The other apps (website, docs, embeds, tenant-router) are
      # already built on PRs by their own Cloudflare Workers builds, and storybook only deploys
      # from its own branch — building everything here just surfaced unrelated, pre-existing rot.
      # `lint` (prettier --check) is also deferred until a repo-wide `pnpm format` is merged.
      - name: Build (api + dashboard)
        env:
          # The dashboard imports PUBLIC_IS_SELFHOSTED from SvelteKit's strict
          # $env/static/public, so it must be DEFINED at build time or the build errors
          # (the Dockerfile sets it via a build arg). Build success doesn't depend on the
          # value, only its presence — so we use "false" here to exercise the default/cloud
          # variant; the self-hosted ("true") build is covered by docker-publish's smoke job.
          PUBLIC_IS_SELFHOSTED: "false"
        run: pnpm turbo run build --filter=@cio/api... --filter=@cio/dashboard... --cache-dir=.turbo
