# PR demo specs (ephemeral)

Playwright specs here are **generated at CI runtime** when the `demo-pr` label is added to a pull request. They are gitignored and not committed.

See `.github/workflows/pr-demo.yml` and `.github/prompts/pr-demo-playwright.md`.
