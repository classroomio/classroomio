# Lesson preview prototypes

Clickable HTML for unenrolled lesson preview (landing hero, Explore modal, teacher flags).

- Open [`index.html`](./index.html) from a local server (`python3 -m http.server` at the repo root).
- Spec: [`prd/lesson-preview/README.md`](../../prd/lesson-preview/README.md)

This folder is not wired into the dashboard.
