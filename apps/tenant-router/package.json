{
  "name": "@cio/tenant-router",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "predev": "pnpm --filter @cio/utils build",
    "deploy": "wrangler deploy",
    "predeploy": "pnpm --filter @cio/utils build",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "pretypecheck": "pnpm --filter @cio/utils build",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@cio/utils": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.5.2",
    "wrangler": "4.21.x"
  }
}
