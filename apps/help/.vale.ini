# Advisory prose linter. Every rule is capped at "warning" so this can
# never fail a build. Mirrors apps/docs/.vale.ini.

StylesPath = styles
MinAlertLevel = suggestion
Packages = Google, write-good

[*.mdx]
BasedOnStyles = Google, write-good
Google.* = warning
write-good.* = warning
